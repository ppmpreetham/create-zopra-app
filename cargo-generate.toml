[template]
cargo_generate_version = ">=0.10.0"

[placeholders]
project-name = { type = "string", prompt = "Name of the project", default = "zopra example" }
project-description = { type = "string", prompt = "Short description of the project", default = "An example generated using the Zopra template" }
